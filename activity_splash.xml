<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center_horizontal"
    android:background="@drawable/bg_gradient"
    tools:context="com.liquiz.utfprliquiz.SplashScreen">

    <!-- UTFPR Logo -->
    <ImageView
        android:id="@+id/utfprLogo"
        android:layout_width="@dimen/splash_logo_width"
        android:layout_height="@dimen/splash_logo_height"
        android:layout_marginTop="@dimen/large_margin"
        android:src="@drawable/utfpr"
        android:contentDescription="@string/utfpr_logo_desc"/>

    <!-- LIQUIZ Logo -->
    <ImageView
        android:id="@+id/appLogo"
        android:layout_width="@dimen/splash_logo_width"
        android:layout_height="@dimen/splash_logo_large_height"
        android:layout_marginTop="@dimen/medium_margin"

        android:src="@drawable/liquiz"
        android:contentDescription="@string/app_logo_desc"/>

    <!-- App Title -->
    <TextView
        android:id="@+id/splash_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/small_margin"
        android:text="@string/app_title"
        android:textColor="@color/black"
        android:textSize="@dimen/splash_title_text_size"
        android:gravity="center"/>
</LinearLayout>